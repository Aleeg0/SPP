Index: MyTestProject/repositories/UserInMemoryRepositoryTest.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using MyProject.dtos;\r\nusing MyProject.entities;\r\nusing MyProject.repositories.Implementations;\r\nusing MyTest;\r\nusing MyTest.attributes;\r\n\r\nnamespace MyTestProject.repositories;\r\n\r\n[TestClass]\r\npublic class UserInMemoryRepositoryTests\r\n{\r\n    private UserInMemoryInMemoryRepository _repository = null!;\r\n    private UserDto _defaultDto = null!;\r\n\r\n    [TestInitialize]\r\n    public void TestInitialize()\r\n    {\r\n        _repository = new UserInMemoryInMemoryRepository();\r\n        _defaultDto = new UserDto(\"testUser\", \"12345678\");\r\n    }\r\n\r\n    [TestCleanup]\r\n    public void TestCleanup()\r\n    {\r\n        Console.WriteLine(\"[Cleanup info]: Test finished, repository cleared from memory.\");\r\n    }\r\n\r\n    [TestMethod(Description = \"Simple Description\")]\r\n    public void Create_ValidDto_ReturnsUserWithNewId()\r\n    {\r\n        var createdUser = _repository.Create(_defaultDto);\r\n\r\n        Assert.IsNotNull(createdUser);\r\n        Assert.AreEqual(0, createdUser.Id);\r\n        Assert.AreEqual(_defaultDto.Login, createdUser.Login);\r\n\r\n        var allUsers = _repository.GetAll();\r\n        Assert.AreEqual(1, allUsers.Count);\r\n    }\r\n\r\n    [TestMethod]\r\n    [DataRow(\"testUser1\", \"secretPassword\")]\r\n    [DataRow(\"admin\", \"verySecretPassword\", IgnoreMessage = \"Skipping this DataRow\")]\r\n    [DataRow(\"admin2\", \"verySecretPassword2\")]\r\n    public void Create_VariousInputs_PopulatesFieldsCorrectly(string login, string password)\r\n    {\r\n        var dto = new UserDto(login, password);\r\n        var user = _repository.Create(dto);\r\n\r\n        Assert.AreEqual(login, user.Login);\r\n        Assert.AreEqual(password, user.Password);\r\n    }\r\n\r\n    [TestMethod]\r\n    public void GetById_ExistingId_ReturnsUser()\r\n    {\r\n        var user = _repository.Create(_defaultDto);\r\n\r\n        var retrievedUser = _repository.GetById(user.Id);\r\n\r\n        Assert.AreEqual(user, retrievedUser);\r\n    }\r\n\r\n    [TestMethod]\r\n    public void GetById_NonExistingId_ReturnsNull()\r\n    {\r\n        var result = _repository.GetById(999);\r\n        Assert.IsNull(result);\r\n    }\r\n\r\n    [TestMethod]\r\n    public void Update_ExistingUser_UpdatesLogin()\r\n    {\r\n        var user = _repository.Create(_defaultDto);\r\n        user.Login = \"new_login\";\r\n\r\n        var updatedUser = _repository.Update(user);\r\n\r\n        Assert.AreEqual(\"new_login\", updatedUser.Login);\r\n        Assert.AreEqual(\"new_login\", _repository.GetById(user.Id).Login);\r\n    }\r\n\r\n    [TestMethod]\r\n    public void Update_NonExistingUser_ThrowsKeyNotFoundException()\r\n    {\r\n        var fakeUser = new User(999, \"fake\", \"fake\");\r\n        Assert.Throws<KeyNotFoundException>(() => _repository.Update(fakeUser));\r\n    }\r\n\r\n    [TestMethod]\r\n    public void Delete_ExistingId_ReturnsTrueAndRemovesUser()\r\n    {\r\n        var user = _repository.Create(_defaultDto);\r\n\r\n        var result = _repository.Delete(user.Id);\r\n\r\n        Assert.IsTrue(result);\r\n        Assert.DoesNotContains(user, _repository.GetAll());\r\n    }\r\n\r\n    [TestMethod]\r\n    public void Delete_NonExistingId_ReturnsFalse()\r\n    {\r\n        var user = _repository.Create(_defaultDto);\r\n        var notExistedUserId = user.Id + 1;\r\n        var result = _repository.Delete(notExistedUserId);\r\n\r\n        Assert.IsFalse(result);\r\n        Assert.Contains(user, _repository.GetAll());\r\n    }\r\n\r\n    [TestMethod]\r\n    [DataRow(\"user1\", \"password1\", \"user2\", \"password2\")]\r\n    public void GetAll_UsersExist_ReturnsAllUsers(string firstLogin, string firstPassword, string secondLogin, string secondPassword)\r\n    {\r\n        _repository.Create(new UserDto(firstLogin, firstPassword));\r\n        _repository.Create(new UserDto(secondLogin, secondPassword));\r\n\r\n        var all = _repository.GetAll();\r\n\r\n        Assert.AreEqual(2, all.Count);\r\n        Assert.Contains(all, u => u.Login == firstLogin);\r\n        Assert.Contains(all, u => u.Password == secondPassword);\r\n    }\r\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/MyTestProject/repositories/UserInMemoryRepositoryTest.cs b/MyTestProject/repositories/UserInMemoryRepositoryTest.cs
--- a/MyTestProject/repositories/UserInMemoryRepositoryTest.cs	(revision e31adc3cd7f98baec0d67154758233956c616e07)
+++ b/MyTestProject/repositories/UserInMemoryRepositoryTest.cs	(date 1771751726517)
@@ -6,7 +6,7 @@
 
 namespace MyTestProject.repositories;
 
-[TestClass]
+
 public class UserInMemoryRepositoryTests
 {
     private UserInMemoryInMemoryRepository _repository = null!;
@@ -19,12 +19,6 @@
         _defaultDto = new UserDto("testUser", "12345678");
     }
 
-    [TestCleanup]
-    public void TestCleanup()
-    {
-        Console.WriteLine("[Cleanup info]: Test finished, repository cleared from memory.");
-    }
-
     [TestMethod(Description = "Simple Description")]
     public void Create_ValidDto_ReturnsUserWithNewId()
     {
@@ -52,13 +46,13 @@
     }
 
     [TestMethod]
-    public void GetById_ExistingId_ReturnsUser()
+    public async Task GetById_ExistingId_ReturnsUser()
     {
         var user = _repository.Create(_defaultDto);
-
         var retrievedUser = _repository.GetById(user.Id);
 
         Assert.AreEqual(user, retrievedUser);
+        await Task.Delay(5000);
     }
 
     [TestMethod]
@@ -111,7 +105,7 @@
 
     [TestMethod]
     [DataRow("user1", "password1", "user2", "password2")]
-    public void GetAll_UsersExist_ReturnsAllUsers(string firstLogin, string firstPassword, string secondLogin, string secondPassword)
+    public async Task GetAll_UsersExist_ReturnsAllUsers(string firstLogin, string firstPassword, string secondLogin, string secondPassword)
     {
         _repository.Create(new UserDto(firstLogin, firstPassword));
         _repository.Create(new UserDto(secondLogin, secondPassword));
@@ -121,5 +115,7 @@
         Assert.AreEqual(2, all.Count);
         Assert.Contains(all, u => u.Login == firstLogin);
         Assert.Contains(all, u => u.Password == secondPassword);
+
+        await Task.Delay(5000);
     }
 }
\ No newline at end of file
Index: MyTestLauncher/Program.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using System.Reflection;\r\nusing MyTestLauncher;\r\n\r\nvar testAssembly = Assembly.LoadFrom(\"MyTestProject.dll\");\r\nvar runner = new TestLauncher();\r\nrunner.LaunchTest(testAssembly);
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/MyTestLauncher/Program.cs b/MyTestLauncher/Program.cs
--- a/MyTestLauncher/Program.cs	(revision e31adc3cd7f98baec0d67154758233956c616e07)
+++ b/MyTestLauncher/Program.cs	(date 1771751036543)
@@ -1,6 +1,38 @@
+using System.Diagnostics;
 using System.Reflection;
 using MyTestLauncher;
 
 var testAssembly = Assembly.LoadFrom("MyTestProject.dll");
-var runner = new TestLauncher();
-runner.LaunchTest(testAssembly);
\ No newline at end of file
+
+Console.WriteLine("=================================================");
+Console.WriteLine("   Сравнение производительности: 1 поток vs 4 потока");
+Console.WriteLine("=================================================\n");
+
+Console.WriteLine(">>> Запуск в 1 поток (Sequential)...");
+
+var sequentialRunner = new TestLauncher(maxDegreeOfParallelism: 1);
+var stopwatch = Stopwatch.StartNew();
+await sequentialRunner.LaunchTestAsync(testAssembly);
+stopwatch.Stop();
+var sequentialTime = stopwatch.Elapsed;
+
+Console.WriteLine($"\n>>> Время выполнения (1 поток): {sequentialTime.TotalSeconds:F3} сек.\n");
+
+Console.WriteLine(new string('-', 50) + "\n");
+
+Console.WriteLine(">>> Запуск в 4 потока (Parallel)...");
+
+var parallelRunner = new TestLauncher(maxDegreeOfParallelism: 4);
+stopwatch.Restart();
+await parallelRunner.LaunchTestAsync(testAssembly);
+stopwatch.Stop();
+var parallelTime = stopwatch.Elapsed;
+
+Console.WriteLine($"\n>>> Время выполнения (4 потока): {parallelTime.TotalSeconds:F3} сек.\n");
+
+
+Console.WriteLine("=================================================");
+Console.WriteLine("                 ИТОГИ");
+Console.WriteLine("=================================================");
+Console.WriteLine($"Последовательно: {sequentialTime.TotalSeconds:F3} с");
+Console.WriteLine($"Параллельно:     {parallelTime.TotalSeconds:F3} с");
\ No newline at end of file
Index: MyTestLauncher/TestLauncher.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>using System.Reflection;\r\nusing MyTest.attributes;\r\nusing MyTest.exceptions;\r\n\r\nnamespace MyTestLauncher;\r\n\r\npublic class TestLauncher\r\n{\r\n    private const ConsoleColor SuccessColor = ConsoleColor.Green;\r\n    private const ConsoleColor TestErrorColor = ConsoleColor.Red;\r\n    private const ConsoleColor CriticalErrorColor = ConsoleColor.DarkRed;\r\n    private const ConsoleColor SkippedColor = ConsoleColor.Cyan;\r\n\r\n    public void LaunchTest(Assembly assembly)\r\n    {\r\n        var assemblyName = assembly.GetName().Name;\r\n        Console.WriteLine($\"=== Launch tests from assembly: {assemblyName} ===\\n\");\r\n        var testClasses = assembly.GetTypes()\r\n            .Where(t => t.GetCustomAttribute<TestClassAttribute>() != null)\r\n            .ToList();\r\n\r\n        testClasses.ForEach(RunTestClass);\r\n    }\r\n\r\n    private void RunTestClass(Type testClass)\r\n    {\r\n        string className = testClass.Name;\r\n        Console.WriteLine($\"--- Class testing: {className} ---\");\r\n\r\n        object? instance;\r\n\r\n        try\r\n        {\r\n            instance = Activator.CreateInstance(testClass);\r\n        }\r\n        catch (Exception ex)\r\n        {\r\n            Console.WriteLine($\"[CRITICAL] Failed to create test class {className}: {ex.Message}\");\r\n            return;\r\n        }\r\n\r\n        if (instance == null)\r\n        {\r\n            Console.WriteLine($\"[CRITICAL] Failed to find test class {className}\");\r\n            return;\r\n        }\r\n\r\n        var classInitializeMethod = testClass.GetMethods(BindingFlags.Static | BindingFlags.Public)\r\n            .SingleOrDefault(m => m.GetCustomAttribute<ClassInitializeAttribute>() != null);\r\n\r\n        var testInitializeMethod = testClass.GetMethods()\r\n            .SingleOrDefault(m => m.GetCustomAttribute<TestInitializeAttribute>() != null);\r\n\r\n        var testCleanupMethod = testClass.GetMethods()\r\n            .SingleOrDefault(m => m.GetCustomAttribute<TestCleanupAttribute>() != null);\r\n\r\n        classInitializeMethod?.Invoke(null, null);\r\n\r\n        var methods = testClass.GetMethods()\r\n            .Where(m => m.GetCustomAttribute<TestMethodAttribute>() != null)\r\n            .ToList();\r\n\r\n        methods.ForEach(m => RunTestMethod(instance, m, testInitializeMethod, testCleanupMethod));\r\n\r\n        var classCleanupMethod = testClass.GetMethods(BindingFlags.Static | BindingFlags.Public)\r\n            .SingleOrDefault(m => m.GetCustomAttribute<ClassCleanupAttribute>() != null);\r\n\r\n        classCleanupMethod?.Invoke(null, null);\r\n\r\n        Console.WriteLine();\r\n    }\r\n\r\n    private void RunTestMethod(object instance, MethodInfo method, MethodInfo? init, MethodInfo? cleanup)\r\n    {\r\n        init?.Invoke(instance, null);\r\n        try\r\n        {\r\n            ExecuteTestMethod(instance, method);\r\n        }\r\n        catch (Exception ex)\r\n        {\r\n            Console.WriteLine($\"[ERROR]: {method.Name}: {ex.Message}\");\r\n        }\r\n        finally\r\n        {\r\n            cleanup?.Invoke(instance, null);\r\n        }\r\n    }\r\n\r\n    private void ExecuteTestMethod(object instance, MethodInfo method)\r\n    {\r\n        var dataRows = method.GetCustomAttributes<DataRowAttribute>().ToList();\r\n\r\n        if (dataRows.Count != 0)\r\n        {\r\n            dataRows.ForEach(dataRow =>\r\n                {\r\n                    if (dataRow.IgnoreMessage != null)\r\n                    {\r\n                        PrintSkipped(method.Name, dataRow.IgnoreMessage);\r\n                    }\r\n                    else\r\n                    {\r\n                        Invoke(instance, method, dataRow.Values);\r\n                    }\r\n                }\r\n            );\r\n        }\r\n        else\r\n        {\r\n            Invoke(instance, method, null);\r\n        }\r\n    }\r\n\r\n    private void Invoke(object instance, MethodInfo method, object[]? args)\r\n    {\r\n        var dataRows = method.GetCustomAttributes<TestMethodAttribute>().FirstOrDefault()!;\r\n        var descriptionString = dataRows.Description != null ? $\"({dataRows.Description})\" : \"\";\r\n        Console.Write($\"{method.Name}{descriptionString}: \");\r\n\r\n        try\r\n        {\r\n            object? result = method.Invoke(instance, args);\r\n            if (result is Task task)\r\n            {\r\n                task.GetAwaiter().GetResult();\r\n            }\r\n\r\n            PrintSuccess();\r\n        }\r\n        catch (TargetInvocationException ex)\r\n        {\r\n            HandleTestException(ex.InnerException ?? ex);\r\n        }\r\n        catch (Exception ex)\r\n        {\r\n            HandleTestException(ex);\r\n        }\r\n    }\r\n\r\n    private void PrintSuccess()\r\n    {\r\n        var prevColor = Console.ForegroundColor;\r\n        Console.ForegroundColor = SuccessColor;\r\n        Console.WriteLine(\"PASSED\");\r\n        Console.ForegroundColor = prevColor;\r\n    }\r\n\r\n    private void PrintSkipped(string methodName, string message)\r\n    {\r\n        var prevColor = Console.ForegroundColor;\r\n        Console.ForegroundColor = SkippedColor;\r\n        Console.Write($\"{methodName}: SKIPPED. \");\r\n        Console.ForegroundColor = prevColor;\r\n        Console.WriteLine(message);\r\n    }\r\n\r\n    private void HandleTestException(Exception ex)\r\n    {\r\n        var prevColor = Console.ForegroundColor;\r\n\r\n        if (ex is TestFailedException)\r\n        {\r\n            Console.ForegroundColor = TestErrorColor;\r\n            Console.WriteLine($\"FAILED. {ex.Message}\");\r\n        }\r\n        else\r\n        {\r\n            Console.ForegroundColor = CriticalErrorColor;\r\n            Console.WriteLine($\"CRASHED. Unexpected error: {ex.GetType().Name} - {ex.Message}\");\r\n        }\r\n\r\n        Console.ForegroundColor = prevColor;\r\n    }\r\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/MyTestLauncher/TestLauncher.cs b/MyTestLauncher/TestLauncher.cs
--- a/MyTestLauncher/TestLauncher.cs	(revision e31adc3cd7f98baec0d67154758233956c616e07)
+++ b/MyTestLauncher/TestLauncher.cs	(date 1771751036538)
@@ -1,31 +1,38 @@
 using System.Reflection;
 using MyTest.attributes;
 using MyTest.exceptions;
+using MyTestLauncher.Utils;
 
 namespace MyTestLauncher;
 
 public class TestLauncher
 {
-    private const ConsoleColor SuccessColor = ConsoleColor.Green;
-    private const ConsoleColor TestErrorColor = ConsoleColor.Red;
-    private const ConsoleColor CriticalErrorColor = ConsoleColor.DarkRed;
-    private const ConsoleColor SkippedColor = ConsoleColor.Cyan;
+    private readonly Logger _logger = new ();
+    private readonly SemaphoreSlim _semaphore;
 
-    public void LaunchTest(Assembly assembly)
+    public TestLauncher(int maxDegreeOfParallelism = 4)
+    {
+        _semaphore = new SemaphoreSlim(maxDegreeOfParallelism);
+    }
+
+    public async Task LaunchTestAsync(Assembly assembly)
     {
         var assemblyName = assembly.GetName().Name;
-        Console.WriteLine($"=== Launch tests from assembly: {assemblyName} ===\n");
+        _logger.Print($"=== Launch tests from assembly: {assemblyName} ===\n");
         var testClasses = assembly.GetTypes()
             .Where(t => t.GetCustomAttribute<TestClassAttribute>() != null)
             .ToList();
 
-        testClasses.ForEach(RunTestClass);
+        foreach (var testClass in testClasses)
+        {
+            await RunTestClassAsync(testClass);
+        }
     }
 
-    private void RunTestClass(Type testClass)
+    private async Task RunTestClassAsync(Type testClass)
     {
         string className = testClass.Name;
-        Console.WriteLine($"--- Class testing: {className} ---");
+        _logger.Print($"--- Class testing: {className} ---");
 
         object? instance;
 
@@ -35,13 +42,13 @@
         }
         catch (Exception ex)
         {
-            Console.WriteLine($"[CRITICAL] Failed to create test class {className}: {ex.Message}");
+            _logger.Print($"[CRITICAL] Failed to create test class {className}: {ex.Message}");
             return;
         }
 
         if (instance == null)
         {
-            Console.WriteLine($"[CRITICAL] Failed to find test class {className}");
+            _logger.Print($"[CRITICAL] Failed to find test class {className}");
             return;
         }
 
@@ -60,116 +67,154 @@
             .Where(m => m.GetCustomAttribute<TestMethodAttribute>() != null)
             .ToList();
 
-        methods.ForEach(m => RunTestMethod(instance, m, testInitializeMethod, testCleanupMethod));
+        bool isNonParallelizable = testClass.GetCustomAttribute<NonParallelizableAttribute>() != null;
+
+        if (isNonParallelizable)
+        {
+            foreach (var method in methods)
+            {
+                await RunTestMethodAsync(instance, method, testInitializeMethod, testCleanupMethod);
+            }
+        }
+        else
+        {
+            var tasks = methods.Select(m =>
+                RunMethodWithSemaphoreAsync(instance, m, testInitializeMethod, testCleanupMethod)
+            ).ToList();
+
+            await Task.WhenAll(tasks);
+        }
 
         var classCleanupMethod = testClass.GetMethods(BindingFlags.Static | BindingFlags.Public)
             .SingleOrDefault(m => m.GetCustomAttribute<ClassCleanupAttribute>() != null);
 
         classCleanupMethod?.Invoke(null, null);
 
-        Console.WriteLine();
+        _logger.Print("");
+    }
+
+    private async Task RunMethodWithSemaphoreAsync(object instance, MethodInfo method, MethodInfo? init, MethodInfo? cleanup)
+    {
+        await _semaphore.WaitAsync();
+        try
+        {
+            await Task.Run(() => RunTestMethodAsync(instance, method, init, cleanup));
+        }
+        finally
+        {
+            _semaphore.Release();
+        }
     }
 
-    private void RunTestMethod(object instance, MethodInfo method, MethodInfo? init, MethodInfo? cleanup)
+    private async Task RunTestMethodAsync(object instance, MethodInfo method, MethodInfo? init, MethodInfo? cleanup)
     {
-        init?.Invoke(instance, null);
+        try
+        {
+            init?.Invoke(instance, null);
+        }
+        catch (Exception ex)
+        {
+            _logger.Print($"[ERROR] Init failed for {method.Name}: {ex.Message}", ConsoleColor.DarkRed);
+            return;
+        }
+
         try
         {
-            ExecuteTestMethod(instance, method);
+            await ExecuteTestMethod(instance, method);
         }
         catch (Exception ex)
         {
-            Console.WriteLine($"[ERROR]: {method.Name}: {ex.Message}");
+            _logger.Print($"[ERROR]: {method.Name}: {ex.Message}");
         }
         finally
         {
-            cleanup?.Invoke(instance, null);
+            try
+            {
+                cleanup?.Invoke(instance, null);
+            }
+            catch (Exception ex)
+            {
+                _logger.Print($"[ERROR] Cleanup failed for {method.Name}: {ex.Message}", ConsoleColor.DarkRed);
+            }
         }
     }
 
-    private void ExecuteTestMethod(object instance, MethodInfo method)
+    private async Task ExecuteTestMethod(object instance, MethodInfo method)
     {
         var dataRows = method.GetCustomAttributes<DataRowAttribute>().ToList();
 
         if (dataRows.Count != 0)
         {
-            dataRows.ForEach(dataRow =>
-                {
-                    if (dataRow.IgnoreMessage != null)
-                    {
-                        PrintSkipped(method.Name, dataRow.IgnoreMessage);
-                    }
-                    else
-                    {
-                        Invoke(instance, method, dataRow.Values);
-                    }
-                }
-            );
+            foreach (var dataRow in dataRows)
+            {
+                if (dataRow.IgnoreMessage != null)
+                {
+                    _logger.PrintSkipped(method.Name, dataRow.IgnoreMessage);
+                }
+                else
+                {
+                    await InvokeTestAsync(instance, method, dataRow.Values);
+                }
+            }
         }
         else
         {
-            Invoke(instance, method, null);
+            await InvokeTestAsync(instance, method, null);
         }
     }
 
-    private void Invoke(object instance, MethodInfo method, object[]? args)
+    private async Task InvokeTestAsync(object instance, MethodInfo method, object[]? args)
     {
         var dataRows = method.GetCustomAttributes<TestMethodAttribute>().FirstOrDefault()!;
         var descriptionString = dataRows.Description != null ? $"({dataRows.Description})" : "";
-        Console.Write($"{method.Name}{descriptionString}: ");
+        string testInfo = $"{method.Name}{descriptionString}";
+
+        var timeoutAttr = method.GetCustomAttribute<TimeoutAttribute>();
 
         try
         {
-            object? result = method.Invoke(instance, args);
-            if (result is Task task)
-            {
-                task.GetAwaiter().GetResult();
-            }
-
-            PrintSuccess();
-        }
-        catch (TargetInvocationException ex)
+            if (timeoutAttr != null)
+            {
+                var testTask = InvokeAsync(instance, method, args);
+                var delayTask = Task.Delay(timeoutAttr.Milliseconds);
+
+                var completedTask = await Task.WhenAny(testTask, delayTask);
+
+                if (completedTask == delayTask)
+                {
+                    throw new TestTimeoutException(timeoutAttr.Milliseconds);
+                }
+
+                await testTask;
+            }
+            else
+            {
+                await InvokeAsync(instance, method, args);
+            }
+
+            _logger.PrintSuccess(testInfo);
+        }
+        catch (TargetInvocationException ex)
+        {
+            var testException = ex.InnerException;
+            if (testException is TestFailedException)
+            {
+                _logger.PrintFailed(testInfo, testException.Message);
+            }
+
+        }
+        catch (Exception ex)
+        {
+            _logger.PrintCrashed(testInfo, ex);
+        }
+    }
+
+    private async Task InvokeAsync(object instance, MethodInfo method, object[]? args)
+    {
+        object? result = method.Invoke(instance, args);
+        if (result is Task task)
         {
-            HandleTestException(ex.InnerException ?? ex);
-        }
-        catch (Exception ex)
-        {
-            HandleTestException(ex);
-        }
-    }
-
-    private void PrintSuccess()
-    {
-        var prevColor = Console.ForegroundColor;
-        Console.ForegroundColor = SuccessColor;
-        Console.WriteLine("PASSED");
-        Console.ForegroundColor = prevColor;
-    }
-
-    private void PrintSkipped(string methodName, string message)
-    {
-        var prevColor = Console.ForegroundColor;
-        Console.ForegroundColor = SkippedColor;
-        Console.Write($"{methodName}: SKIPPED. ");
-        Console.ForegroundColor = prevColor;
-        Console.WriteLine(message);
-    }
-
-    private void HandleTestException(Exception ex)
-    {
-        var prevColor = Console.ForegroundColor;
-
-        if (ex is TestFailedException)
-        {
-            Console.ForegroundColor = TestErrorColor;
-            Console.WriteLine($"FAILED. {ex.Message}");
+            await task;
         }
-        else
-        {
-            Console.ForegroundColor = CriticalErrorColor;
-            Console.WriteLine($"CRASHED. Unexpected error: {ex.GetType().Name} - {ex.Message}");
-        }
-
-        Console.ForegroundColor = prevColor;
     }
 }
\ No newline at end of file
Index: MyTestLauncher/Utils/Logger.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/MyTestLauncher/Utils/Logger.cs b/MyTestLauncher/Utils/Logger.cs
new file mode 100644
--- /dev/null	(date 1771747445268)
+++ b/MyTestLauncher/Utils/Logger.cs	(date 1771747445268)
@@ -0,0 +1,83 @@
+using MyTest.exceptions;
+
+namespace MyTestLauncher.Utils;
+
+public class Logger
+{
+    // private const ConsoleColor SuccessColor = ConsoleColor.Green;
+    // private const ConsoleColor TestErrorColor = ConsoleColor.Red;
+    // private const ConsoleColor CriticalErrorColor = ConsoleColor.DarkRed;
+    // private const ConsoleColor SkippedColor = ConsoleColor.Cyan;
+    private readonly Lock _consoleLock = new();
+
+    public ConsoleColor SuccessColor { get; init; } = ConsoleColor.Green;
+    public ConsoleColor TestErrorColor { get; init; } = ConsoleColor.Red;
+    public ConsoleColor CriticalErrorColor { get; init; } = ConsoleColor.DarkRed;
+    public ConsoleColor SkippedColor { get; init; } = ConsoleColor.Cyan;
+
+    public void Print(string message, ConsoleColor? color = null)
+    {
+        lock (_consoleLock)
+        {
+            if (color.HasValue)
+            {
+                var prev = Console.ForegroundColor;
+                Console.ForegroundColor = color.Value;
+                Console.WriteLine(message);
+                Console.ForegroundColor = prev;
+            }
+            else
+            {
+                Console.WriteLine(message);
+            }
+        }
+    }
+
+    public void PrintSuccess(string testInfo)
+    {
+        lock (_consoleLock)
+        {
+            var prevColor = Console.ForegroundColor;
+            Console.Write($"{testInfo}: ");
+            Console.ForegroundColor = SuccessColor;
+            Console.WriteLine("PASSED");
+            Console.ForegroundColor = prevColor;
+        }
+    }
+
+    public void PrintSkipped(string methodName, string message)
+    {
+        lock (_consoleLock)
+        {
+            var prevColor = Console.ForegroundColor;
+            Console.ForegroundColor = SkippedColor;
+            Console.Write($"{methodName}: SKIPPED. ");
+            Console.ForegroundColor = prevColor;
+            Console.WriteLine(message);
+        }
+    }
+
+    public void PrintFailed(string testInfo, string message)
+    {
+        lock (_consoleLock)
+        {
+            var prevColor = Console.ForegroundColor;
+            Console.Write($"{testInfo}: ");
+            Console.ForegroundColor = TestErrorColor;
+            Console.WriteLine($"FAILED. {message}");
+            Console.ForegroundColor = prevColor;
+        }
+    }
+
+    public void PrintCrashed(string testInfo, Exception ex)
+    {
+        lock (_consoleLock)
+        {
+            var prevColor = Console.ForegroundColor;
+            Console.Write($"{testInfo}: ");
+            Console.ForegroundColor = CriticalErrorColor;
+            Console.WriteLine($"CRASHED. Unexpected error: {ex.GetType().Name} - {ex.Message}");
+            Console.ForegroundColor = prevColor;
+        }
+    }
+}
\ No newline at end of file
Index: MyTest/attributes/TimeoutAttribute.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/MyTest/attributes/TimeoutAttribute.cs b/MyTest/attributes/TimeoutAttribute.cs
new file mode 100644
--- /dev/null	(date 1771749438949)
+++ b/MyTest/attributes/TimeoutAttribute.cs	(date 1771749438949)
@@ -0,0 +1,7 @@
+namespace MyTest.attributes;
+
+[AttributeUsage(AttributeTargets.Method)]
+public class TimeoutAttribute(int milliseconds) : Attribute
+{
+    public int Milliseconds { get; } = milliseconds;
+}
\ No newline at end of file
Index: MyTest/attributes/NonParallelizableAttribute.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/MyTest/attributes/NonParallelizableAttribute.cs b/MyTest/attributes/NonParallelizableAttribute.cs
new file mode 100644
--- /dev/null	(date 1771749438083)
+++ b/MyTest/attributes/NonParallelizableAttribute.cs	(date 1771749438083)
@@ -0,0 +1,7 @@
+namespace MyTest.attributes;
+
+[AttributeUsage(AttributeTargets.Class)]
+public class NonParallelizableAttribute: Attribute
+{
+
+}
\ No newline at end of file
Index: .idea/.idea.Lab2/.idea/.gitignore
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/.idea.Lab2/.idea/.gitignore b/.idea/.idea.Lab2/.idea/.gitignore
new file mode 100644
--- /dev/null	(date 1771744705223)
+++ b/.idea/.idea.Lab2/.idea/.gitignore	(date 1771744705223)
@@ -0,0 +1,15 @@
+# Default ignored files
+/shelf/
+/workspace.xml
+# Rider ignored files
+/contentModel.xml
+/modules.xml
+/projectSettingsUpdater.xml
+/.idea.Lab1.iml
+# Ignored default folder with query files
+/queries/
+# Datasource local storage ignored files
+/dataSources/
+/dataSources.local.xml
+# Editor-based HTTP Client requests
+/httpRequests/
Index: MyTest/exceptions/TestTimeoutException.cs
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/MyTest/exceptions/TestTimeoutException.cs b/MyTest/exceptions/TestTimeoutException.cs
new file mode 100644
--- /dev/null	(date 1771750742059)
+++ b/MyTest/exceptions/TestTimeoutException.cs	(date 1771750742059)
@@ -0,0 +1,3 @@
+namespace MyTest.exceptions;
+
+public class TestTimeoutException(int limit) : Exception($"Test execution timed out. Limit: {limit}ms");
Index: .idea/.idea.Lab2/.idea/indexLayout.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/.idea.Lab2/.idea/indexLayout.xml b/.idea/.idea.Lab2/.idea/indexLayout.xml
new file mode 100644
--- /dev/null	(date 1771744700512)
+++ b/.idea/.idea.Lab2/.idea/indexLayout.xml	(date 1771744700512)
@@ -0,0 +1,8 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="UserContentModel">
+    <attachedFolders />
+    <explicitIncludes />
+    <explicitExcludes />
+  </component>
+</project>
\ No newline at end of file
Index: .idea/.idea.Lab2/.idea/vcs.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/.idea.Lab2/.idea/vcs.xml b/.idea/.idea.Lab2/.idea/vcs.xml
new file mode 100644
--- /dev/null	(date 1771744721146)
+++ b/.idea/.idea.Lab2/.idea/vcs.xml	(date 1771744721146)
@@ -0,0 +1,6 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="VcsDirectoryMappings">
+    <mapping directory="$PROJECT_DIR$/.." vcs="Git" />
+  </component>
+</project>
\ No newline at end of file
